#!/usr/bin/env php

<?php

$autoloadPath1 = __DIR__ . '/../../../autoload.php';

$autoloadPath2 = __DIR__ . '/../vendor/autoload.php';

if (file_exists($autoloadPath1)) {
    require_once $autoloadPath1;
} else {
    require_once $autoloadPath2;
}

use function BrainGames\Cli\welcomeUser;
use function BrainGames\Even\evenGame;
use function BrainGames\Calc\getValues;
use function BrainGames\Calc\countValues;
use function BrainGames\Calc\getAnswer;
use function BrainGames\Calc\checkAnswer;
use function BrainGames\Calc\calcGame;

// $values = getValues();

// countValues($values);
// $result = (countValues($values)[3]);

// $answer = getAnswer();

// checkAnswer($answer, $result);

$userName = welcomeUser();

// evenGame($userName);
calcGame($userName);